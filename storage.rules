rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /template-previews/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == 'araiakhylbek78@gmail.com';
    }
  }
}
